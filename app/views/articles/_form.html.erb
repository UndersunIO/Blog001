<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <%= form_for(@article, :html => {class: "form-horizontal", role: "form"}) do |form| %>
      <div class="form-group">
        <div class="col-sm-2 control-label">
            <%= form.label :title %>
        </div>
        <div class="col-sm-10">
            <%= form.text_field :title, class: "form-control", placeholder: "Titre de l'article" %>
        </div>
      </div>

    <div class="form-group">
          <div class="col-sm-2 control-label">
            <%= form.label :description %>
          </div>
          <div class="col-sm-10">
            <%= form.text_area :description,rows: 10, class: "form-control", placeholder: "Contenu de l'article" %>
          </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <%= form.submit class:" btn btn-default" %>
        </div>
      </div>
    <% end %>
  </div>

<% end %>
